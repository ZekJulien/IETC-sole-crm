export enum TIME_ENTRY_CHANNELS {
  GET_ALL        = 'time-entry:get-all',
  SUM_BY_PROJECT = 'time-entry:sum-by-project',
  SUM_BY_MONTH   = 'time-entry:sum-by-month',
  ADD            = 'time-entry:add',
  UPDATE         = 'time-entry:update',
  REMOVE         = 'time-entry:remove',
}
